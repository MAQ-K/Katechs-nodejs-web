# 2026-09-07

## Content & Data Manager — team briefs for every remaining service page

Wrote one `brief.md` per service (designer/content-creator/video-editor audience, not code-pipeline
framing), matching the depth/rigor of the existing `data/hosting-services/brief.md` model. Read every
live page file, its components, and its data source (where one exists) before writing — no invented
prices, plans, stats, or client data anywhere.

**Files written:**
- `data/app-development/brief.md`
- `data/digital-marketing/brief.md`
- `data/emails/brief.md`
- `data/seo/brief.md`
- `data/vps-hosting/brief.md`
- `data/wordprees-hosting/brief.md`
- `data/security/brief.md` (new folder — no `data/security/` existed before)
- `data/ssl-certificate/brief.md` (new folder)
- `data/website-design/brief.md` (new folder)
- `data/services/brief.md` — the Web Services hub, covering all 4 built areas (business websites,
  WordPress, e-commerce, consulting) in one brief since they share `data/services/data.js`

**Notable findings, for whoever picks these up next:**
- `security`, `ssl-certificate`, `website-design` have **no `data.js`** — legacy template pages, content
  hardcoded in components. `security` in particular reads as **third-party vendor marketing copy**
  (Veritas Backup Exec, Sophos XG/XDR terminology) with no real Katechs-specific framing and **zero
  pricing of any kind** — flagged as needing a business decision, not a content edit.
- `vps-hosting` and `wordprees-hosting`: 🔴 every order button links to `https://clients.knoztech.com/...`
  (a different company's domain, not Katechs), and the shared `hostingFeature.js` component's body copy
  literally says "كنوز تك" instead of Katechs — both flagged as urgent, not silently fixed.
- `ssl-certificate` by contrast is real, consistent content (actual CA products/prices from RapidSSL/
  GeoTrust/DigiCert) and correctly links to `clients.katechs.com` — used as the "what correct looks like"
  reference in the other two hosting briefs.
- `app-development`, `digital-marketing`, and `seo` all have finished `Pricing.js` (and DM also has
  `TrustStrip.js`) components sitting unused — written but not imported into their pages. Flagged per
  page rather than assumed intentional.
- `website-design` (legacy standalone page) significantly overlaps with the new Web Services hub's
  "مواقع الشركات"/"ووردبريس" areas — flagged for a business decision on which stays live.
- Web Services hub: 24 prices (Area 2 + Area 3, EG+SA, 3 tiers each) are still `TODO(prices)` placeholders
  — the single biggest content gap found across every page reviewed today.

No JSX/SCSS touched. No git run. Paths above are everything changed.
