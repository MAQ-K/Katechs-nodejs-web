# 2026-09-06

## Removed "القنوات التي نعمل عليها" from the homepage marketing section

That heading (and its subtitle "لا نستخدم كل القنوات لكل عميل — نختار ما يناسب جمهورك وميزانيتك")
lives in `components/DigitalMarketing/Channels.js`, which **`/services/digital-marketing` also
renders** — so deleting it would have stripped the marketing page's own section title too.

Gated instead: new optional **`showHead`**, default `true`, and `/hp-new` passes `false`. Third
optional-prop gate on a shared component in this rebuild, after `FaqHorizontal limit` and
`Channels showNote`. The pattern holds: never cut shared content, add a prop with the old behaviour
as the default.

Read the request as "remove that heading", not "remove the marketing section" — the section itself
was added a turn earlier at the user's request, and the six cards still carry it. Easy to reverse if
the whole section was meant.

### Verified
`/hp-new`: heading 0, subtitle 0, six cards intact, `id="marketing"` still present.
`/services/digital-marketing`: 200, heading 1, subtitle 1, internal note 1, cards unchanged. `/` 200.

### ⚠️ The repo moved drive — and this changes an earlier finding
The working copy is now **`d:\Work\katechs-nodejs-web`** (user `ELTANANY01007675193`). The `g:` path
this session had been using is gone. All session work is intact in the d: copy.

**This corrects yesterday's note in `logs/2026-09-05.md`.** That entry claimed `ui-ux-pro-max` and
`design-taste-frontend` are "NOT INSTALLED on this machine" and that SOURCES.md pointed at the wrong
user. That was true of the `g:`/`France Tech` environment the session was in at the time — **it is
not true here.** On this machine both skills are present and available, exactly where SOURCES.md
says. The WhyChooseUs section built yesterday was therefore designed without them; worth a design
pass against `ui-ux-pro-max` + `design-taste-frontend` now that they are reachable.

### Paths changed
- `Web/Backup/ar/components/DigitalMarketing/Channels.js` (optional `showHead`)
- `Web/Backup/ar/pages/hp-new.js` (passes `showHead={false}`)

## …then removed the whole marketing section

"REMOVE THIS SECTION" (screenshot of the six navy channel cards). So the channels grid is off
`/hp-new` entirely — the heading gating an hour earlier is moot.

- `pages/hp-new.js`: dropped the `<section id="marketing">` block and the `Channels` import.
- **`components/DigitalMarketing/Channels.js` reverted to its original form.** Both props I had
  added for the homepage (`showNote`, `showHead`) existed only for a consumer that no longer exists,
  and dead parameters in a shared component are clutter I created. Removing them changes nothing
  anywhere: both defaulted to `true`, which is what the marketing page always rendered.
- **`marketing` is a placeholder slot again.** The nav pill stays — the user fixed those five — so
  the `id` has to keep existing or the scroll-spy never matches it and its jump silently does
  nothing (that failure mode is exactly the bug chased on 2026-09-04).

One consequence worth noting: the placeholder now renders **after** `why-us`, where the real section
sat **before** it. Nav order still matches DOM order for all five pills, so the spy stays correct.

### Verified
`/hp-new`: 0 `dm-card`, 0 `dm-section`, `id="marketing"` still present, placeholder label showing.
`/services/digital-marketing`: 200 with heading, subtitle, note and all its cards intact — fully
back to how it was before any of this. `/` and `/about-us/` 200.

### Still open
The internal "سؤال مفتوح: أي منصات إعلانية نذكرها بالاسم" note is live on
`/services/digital-marketing` again, since the gate is gone. It was never answered.

### Paths changed
- `Web/Backup/ar/pages/hp-new.js`
- `Web/Backup/ar/components/DigitalMarketing/Channels.js` (reverted to original)
