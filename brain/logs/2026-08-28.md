# 2026-08-28

## UI Implementer — Services Hero restyle (T-014e)

Restyled `.wsv-hero` to match `brain/ui-library/inspiration/web services  page hero .png`, keeping the
existing video/image background, overlay, Arabic copy, and slide-rotation logic untouched.

**Files changed:**
- `components/Services/Hero.js` — added a `bx-chevron-left` icon to the badge (placed after the label in
  markup so it lands on the visual left under RTL, mirroring the reference's forward-pointing chevron
  instead of literally copying its direction). Added `.wsv-hero-arc-glow` div above the existing sweep SVG;
  bumped the sweep's `viewBox`/curve height from 80 to 120 to give the glow room to sit above it.
- `styles/style.scss` (`.wsv-hero*` block, ~13382–13600, held under the `styles-style-scss` lock):
  - `.wsv-hero-badge`: glassy pill (`rgba(255,255,255,.1)` bg, `backdrop-filter: blur(6px)`) replacing the
    cyan-tinted pill; chevron colored `$main-color`.
  - `.wsv-hero-text h1`: `clamp(28px,4vw,46px)` → `clamp(36px,6vw,72px)`, tighter line-height/letter-spacing.
  - `.wsv-hero-text p`: capped to `max-width: 640px`, bumped to 17px.
  - `.wsv-btn` (primary): now solid white/near-navy-text rounded-rect with a drop shadow — the reference's
    "one dominant button," was previously the same cyan as everywhere else on the site.
  - `.wsv-btn-ghost` (secondary): dropped the border/background entirely, now reads as an underlined text
    link, so the primary stays the only dominant shape even though we carry an extra CTA the reference
    doesn't have.
  - New `.wsv-hero-arc-glow`: blurred radial-gradient dark shadow (`filter: blur(20px)`) sitting above the
    existing flat `.wsv-hero-sweep` curve, approximating the reference's soft gradient-shadow arc while the
    flat curve underneath still guarantees a clean seam into `$wsv-band` (#f5f6f8, the Projects strip's
    actual background — confirmed via `pages/services/index.js`, not the reference's white).
  - Mobile overrides (~13757 block): added `.wsv-hero-arc-glow` scale-down, bumped `.wsv-hero-sweep` height
    45→60px and `.wsv-hero-dots-wrap` bottom accordingly.
  - Recompiled `styles/style.css` + `.css.map` via `npx sass styles/style.scss styles/style.css
    --source-map --style=expanded` (clean compile, no errors).

**Verification:** cleared `.next` (had a stale trace-file lock from a prior process — killed stray
`node.exe` first), ran `node server.js`, confirmed `/services/` returns 200 and the response HTML contains
`wsv-hero-badge`, `bx-chevron-left`, `wsv-hero-arc-glow`, `wsv-hero-sweep`. No new console/server errors.
Dev server stopped afterward.

**Not touched:** background video/image, overlay tint, `data/services/data.js` copy, dots logic, section
order, any other page.

**Registry:** added `Services Hero` row to `brain/components/REGISTRY.md`.

**Lock:** claimed and released `brain/locks/style.scss.lock.md` (was free at start, no one else touched
`.wsv-hero*` during this run).
