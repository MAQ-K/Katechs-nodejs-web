# 2026-08-25

## UI Implementer — SEO page reskinned to AppDev's visual system
Task: apply App Development's card/border/button/font theme to every existing SEO section,
keeping SEO's own layout, section list and Arabic copy untouched.

Diffed `components/AppDev/Hero.js` vs `components/Seo/Hero.js` and the two SCSS blocks
(`.app-*` ~L7581-9800, `.seo-*` ~L9804-10820 in `styles/style.scss`) before touching anything.
Findings: AppDev cards carry no visible border and rely entirely on a layered "soft emboss"
box-shadow (ambient shadow + a light/dark inset hairline pair) at 16px radius, with a
translateY(-6px) + stronger-shadow hover. Buttons are 10px rounded-rectangles
(`.default-btn` / `.app-hero-ghost-btn`), never full pills. Headings are Cairo/700. SEO was
using 1px `$seo-line` borders everywhere instead of shadow, 18px card radius, fully-pill
(100px) CTAs, and Cairo/800 headings — a different, "flat editorial" system.

### Changed (all in `styles/style.scss`, in place inside the existing SEO block, no new
banner appended)
- `.seo-h1`/`.seo-h2` font-weight 800 → 700; `.seo-stat-value` 800 → 700 (matches
  `.app-stat-num`).
- `.seo-card` / `.seo-card-dark`: dropped the `1px solid $seo-line` border, radius 18px → 16px,
  replaced the flat hover shadow with AppDev's layered emboss shadow (rest + hover states),
  hover lift -5px → -6px. `.seo-plan` (Pricing) inherits this automatically since it's applied
  as `seo-card seo-plan` in `Pricing.js`.
- `.seo-icon-well` radius 13px → 14px (matches `.app-plan-icon`).
- `.seo-media-panel` (Hero/AiSearch/Reporting split panel): border removed, radius 22px → 24px
  (matches `.app-hero-frame`), added AppDev's ambient plate shadow.
- `.seo-serp-card`, `.seo-skel-card`, `.seo-report-card` (the three mock/illustration cards):
  border removed, radius 18px → 16px, single flat shadow replaced with the same card emboss
  shadow as `.seo-card`.
- `.seo-accordion .accordion__item` (Faq): border removed, radius unchanged (16px, already
  matched), replaced with `.app-faq-accordion`'s shadow.
- `.seo-btn`: radius 100px (full pill) → 10px, padding 14px/28px → 16px/30px (AppDev CTA
  shape). Kept SEO's own black fill/hover-brightness treatment — color wasn't in the "borders,
  cards, btns, font" ask and AppDev has no black-CTA equivalent to copy from.
  `.seo-btn-invert`/`.seo-plan .is-popular .seo-btn` inherit the new radius automatically.
- `.seo-btn-ghost`: border 1.5px → 1px, hover swapped from darkening-to-ink to lifting to
  `$main-color` border+text (mirrors `.app-hero-ghost-btn`'s hover, and ties the ghost button
  to the same cyan accent already used for icon wells/SERP rank/dots elsewhere on the page).

### Left alone (explicitly out of scope or no AppDev equivalent)
- `.seo-eyebrow`, `.seo-proof-row`, `.seo-tag`, `.seo-flag`, `.seo-input`, `.seo-plan-badge`,
  `.seo-skel-badge` — small bordered pills/callouts/inputs. AppDev's own small badges
  (`.app-plan-badge`) are pills too, so no conflict; AppDev has no warning-flag or form-input
  equivalent to source a "same theme" value from.
- Section background rhythm (white / `$seo-gray` / `$seo-black` alternation) — already
  conceptually identical to AppDev's white / `#f5f6f8` / dark alternation, left as-is.
- `.seo-btn` fill color (black) and dark-band variants (`.seo-dark .seo-btn-ghost`) — color
  wasn't asked for, only shape/border/shadow.

### Verified
- `npx sass styles/style.scss styles/style.css --source-map` — compiled clean, no errors.
- Existing dev server on :3000 was returning a stale 404 for `/services/seo/` (app-development
  and `/` were fine on it) — cleared `.next/cache` and started a second instance on
  `port=3099` per `brain/PARALLEL.md`'s stuck-port guidance rather than touch the other
  session. `http://localhost:3099/services/seo/` → 200, compiled with 0 errors, HTML confirmed
  `dir="rtl"`/`lang="ar"` intact and the new classes (`seo-card`, `seo-btn`, `seo-h1`) present.
  Grepped no hardcoded inline `boxShadow`/`borderRadius`/`background` in any `components/Seo/*`
  JSX — everything is class-driven, so the SCSS edit is the whole surface.

### Files touched
- `Web/Backup/ar/styles/style.scss` (SEO block only, in place)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)

No `components/Seo/*.js` or `pages/services/seo/index.js` edits were needed — the whole
"theme" lives in the SCSS classes those components already reference.

## UI Implementer — Digital Marketing (T-011) reskinned to AppDev's visual system
Task: rebuild the `.dm-*` SCSS block (styles/style.scss, ~L10927-11963) in place so DM's
cards/buttons/borders/font match `components/AppDev/**` exactly. `components/DigitalMarketing/*.js`
already reference `dm-*` classes only (no inline styles/hardcoded colors), so no JSX changes
were needed — this was a pure token swap inside the existing block.

### Values pulled from AppDev (not approximated)
- Card: `.app-feature-card` → 16px radius, `1px solid rgba(15,23,42,.08)`, no border-color/lift
  on hover, hover = `0 16px 30px -20px rgba(15,23,42,.18)` only. Applied to `.dm-card`.
- Icon well: `.app-feature-icon` → 40x40, 10px radius, `#eef7fb` bg, `$main-color` icon (cyan at
  rest, not just on hover). Applied to `.dm-icon-well`.
- Buttons: `.default-btn` (10px radius, `$main-color` fill) for `.dm-btn`; `.app-hero-ghost-btn`
  (10px radius, `1px solid #d9dde3`, hover → cyan border+text) for `.dm-btn-ghost`.
- Pricing cards: `.app-plan-card`'s soft-emboss box-shadow recipe + translateY(-6px) hover,
  applied to `.dm-plan` (previously flat-bordered). `.is-popular` → `#0a1f44` navy
  (`.app-plan-card.is-popular`), was `$seo-black`.
- FAQ: `.app-faq-accordion` → box-shadow instead of border on `.accordion__item`, icon circle
  cyan-tinted at rest (32px, was 30px gray-at-rest).
- Feature-list icons (`.dm-list li i`): swapped plain colored `bx-check` for
  `.app-plan-features li i`'s 20px cyan-tinted circle badge.
- Headings: `.dm-h1/h2/h3`/`.dm-stat-value` font-weight 800 → 700 (AppDev headings are all 700).
- Added `$dm-line`, `$dm-ink`, `$dm-ink-dim`, `$dm-surface-alt`, `$dm-dark` (#0b0b0d, AppDev's
  dark band), `$dm-navy` (#0a1f44), `$dm-radius`(16px), `$dm-radius-sm`(10px) — AppDev's own
  values, scoped to the DM block only; did not touch `$seo-*` tokens or the SEO block.
- No direct AppDev equivalent for a dark chip/card: kept `#111318` (AppDev's own
  `.app-fv-bubble` dark-chip color) for `.dm-campaign-badge` and `.dm-card-dark` rather than
  inventing a new value.

### Verified
- `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — compiled clean,
  0 errors (first attempt used `--style=compressed` by mistake, collapsing the whole file to one
  line; recompiled expanded to match the existing convention before checking anything in).
- Port 3000's existing dev instance is the same stale one noted in the SEO entry above (still
  had no working `pages-manifest.json` after this session's `.next/cache` clear); did not
  restart it. Started a throwaway instance on `port=3001` instead, confirmed
  `http://localhost:3001/services/digital-marketing` → 200, all `dm-*` classes present in the
  HTML, no console/hydration errors, then killed that instance (PID 2272) when done. Grepped
  compiled `style.css` for `.dm-card` to confirm the new 16px/`rgba(15,23,42,.08)` values
  actually landed.
- `brain/locks/styles-style-scss.lock.md` was already released by the SEO session before I
  started editing — no lock conflict, nothing left to clean up.

### Files touched
- `Web/Backup/ar/styles/style.scss` (`.dm-*` block only, in place, no new banner)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)

No `components/DigitalMarketing/*.js` or `pages/services/digital-marketing/index.js` edits.

## UI Implementer — SEO page color palette matched to App Development
Follow-up to the two entries above: same border/card/btn-shape reskin, now the ask is
specifically color ("make the SEO page the same color palette as App Dev"). Diffed
`.app-*`'s actual color values (not just class names) against `.seo-*`'s.

### Scope guard — did NOT touch the shared `$seo-*` tokens
The `.seo-*` block's own `$seo-black/$seo-gray/$seo-card-dark/$seo-ink/$seo-ink-dim/$seo-line/
$seo-line-dark` variables turned out to be reused directly by the **Training** page's `.tr-*`
block further down the same file ("Same design system as SEO" — a coupling that postdates the
comment above the SEO banner claiming Training was still on `ux-prototype.css`). Editing those
variables in place would have silently reskinned Training too, which wasn't part of this ask and
wasn't verified. Left the original `$seo-*` declarations untouched and added a parallel
`$seopal-*` ("SEO palette") variable set holding the actual AppDev-equivalent values, then
scoped-replaced (`sed` restricted to the `.seo-section { … }` → end-of-SEO-block line range only,
verified after with a grep that zero `$seopal-*` usages leaked outside that range) every
in-block reference from `$seo-*` to `$seopal-*`. Confirmed via compiled CSS: `.seo-section.seo-dark`
→ `#0b0b0d` (new), `.tr-section.tr-dark` → `#1b1d23` (unchanged, still Training's original value).

### Changed (all in `styles/style.scss`, values only — no selectors added/removed, no shadows/
radii/borders touched from the prior structural pass)
- **Section backgrounds**: `$seopal-gray` (alt band) `#f6f6f4` → `#f5f6f8`, AppDev's exact
  `.app-hero`/`.app-pricing` alt tone. `$seopal-black` (dark band) `#1b1d23` → `#0b0b0d`, AppDev's
  exact `.app-platforms`/`.app-hero-media` dark tone.
- **New: `$seopal-navy` (#0a1f44)**, AppDev's separate "most popular" plan tone
  (`.app-plan-card.is-popular`). SEO's `.seo-plan.is-popular` previously reused the same dark as
  the page's dark section bands — now split, matching AppDev where the dark band and the popular
  plan card are two different colors, not one.
- **Text scale**: `$seopal-ink` (#101012 → `$heading-color` #212121), `$seopal-ink-dim` (#6c6c72 →
  `$body-color` #4d4d4d) — headings/body text now literally share AppDev's color values instead of
  SEO's own darker/independent scale.
- **Dark on-dark card**: `$seopal-card-dark` (#24262d → #111318), AppDev's own dark-chip color
  (`.app-fv-bubble`) — no cyan/navy AppDev card-on-dark existed to source from, reused the exact
  value the DM pass already borrowed for the same situation.
- **Borders/dividers**: `$seopal-line` (rgba(12,12,14,.1) → rgba(15,23,42,.08), AppDev's literal
  hairline value), `$seopal-line-dark` (rgba(255,255,255,.1) → rgba(255,255,255,.12), matching
  `$dm-line-dark`).
- **Icon wells** (`.seo-icon-well`): was a neutral grey at rest (rgba(12,12,14,.05)) that only
  turned cyan on card hover; AppDev's icon wells (`.app-feature-icon`/`.app-plan-icon`) are
  cyan-tinted at rest always. Changed rest state to `#eef7fb` bg / `$main-color` icon (matches
  AppDev exactly), hover deepens to rgba(29,211,248,.22) instead of "revealing" cyan for the first
  time. `.seo-card-dark .seo-icon-well` now rgba(255,255,255,.1) bg / `$main-color` icon, matching
  `.app-plan-card.is-popular .app-plan-icon` exactly (bg lightens, icon stays cyan not white).
- **Badges/pills**: `.seo-plan-badge` was a soft cyan-tint pill (rgba(29,211,248,.16) bg + cyan
  text) — AppDev's own `.app-plan-badge` is a solid cyan fill + white text; matched that pairing.
  `.seo-acc-sign` (FAQ icon circle) was neutral grey at rest, cyan only when expanded — AppDev's
  `.app-faq-accordion i` is cyan-tinted at rest always with no toggle color-change; matched that,
  the rotate-on-expand transform (existing motion) is untouched.
- **Dropped two non-palette dark-teal values with no AppDev source**: `#0a90ac` on
  `.seo-card:hover .seo-icon-well` and `.seo-skel-badge` → both now `$main-color`.
- **Muted label**: `.seo-step-num` color rgba(12,12,14,.32) (derived from SEO's old ink) →
  `#9aa2ad`, AppDev's own standalone muted-label color (`.app-step-label`), not a faded heading tint.

### CTA button color decision — asked for, not guessed
`.seo-btn`'s black fill (`$seo-black`) was explicitly left alone in the prior structural pass
("color wasn't part of the ask"). Now that color is the ask: **switched it to `$main-color`**
(AppDev's own primary CTA fill — `.default-btn` is cyan everywhere on that page, no black-CTA
equivalent exists there to match instead). Reasoning: the whole point of this task is "same color
palette as App Dev," and AppDev has no black anywhere in its primary-action color — keeping black
would have left the single most visually prominent element on the page (the main CTA) out of the
palette match while everything around it moved to cyan. Text stays white, shape/radius/padding
untouched from the prior pass, hover kept the existing brightness-lift + lift-and-shadow motion but
re-keyed the shadow color from near-black (rgba(12,12,14,.4)) to cyan (rgba(29,211,248,.5)) so the
glow reads as the button's own color, not a leftover from the black-fill era, and eased the
brightness lift down from 1.22 to 1.1 since a strong brightness filter on cyan (unlike on black)
washes toward white. `.seo-btn-invert` (used on dark bands via `AuditForm.js`/`CtaBand.js`) and the
`.seo-plan.is-popular .seo-btn` white/navy inversion were left as their existing structural
pattern — both already read correctly against the new dark/navy tones since they reference the
same variables.

### Left alone (no AppDev equivalent, or intentionally kept as SEO's own semantic color)
- `.seo-flag` warning amber (#f5a524) and `.seo-error`/`seo-input-error` red (#ff6b6b/#ff8a8a) —
  semantic warning/error colors, not brand palette; AppDev has no such states to source from.
- `.seo-serp-badge`'s box-shadow (rgba(12,12,14,.5)) — a neutral ambient shadow tint, not a brand
  color; left as-is rather than force it to AppDev's rgba(15,23,42,…) shadow family for a
  cosmetic-only shadow-color tweak that wasn't asked for.

### Verified
- `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — compiled clean,
  0 errors, twice (once mid-pass, once final).
- Grepped compiled `style.css` directly for representative selectors before/after:
  `.seo-section.seo-dark` → `background: #0b0b0d` (was `#1b1d23`), `.seo-plan.is-popular` →
  `background: #0a1f44` (new, was `#1b1d23`), `.seo-icon-well` → `background: #eef7fb; color:
  #1dd3f8` (was grey/ink), `.seo-plan-badge` → `background: #1dd3f8; color: #ffffff` (was a soft
  tint). Cross-checked Training's `.tr-section.tr-dark` still compiles to `#1b1d23` and
  `.tr-icon-well` still `rgba(12,12,14,.05)` — confirmed unaffected.
- Live: existing dev instance on `port=3099` (from the prior pass, still running) — 
  `http://localhost:3099/services/seo/` → 200 (one transient 404 mid-recompile, resolved after
  ~3s and returned 200), `dir="rtl"`/`lang="ar"` intact, `seo-btn`/`seo-card`/`seo-h1`/
  `seo-icon-well`/`seo-plan-badge` all present in the rendered HTML.
  `http://localhost:3099/services/app-development/` → 200 throughout (untouched page, sanity
  check). Did not restart or otherwise touch that dev instance beyond letting it recompile.
- Held `brain/locks/styles-style-scss.lock.md` for the duration, released on completion.

### Files touched
- `Web/Backup/ar/styles/style.scss` (SEO block's color values only — new `$seopal-*` variables +
  in-block references swapped to them; original `$seo-*` declarations at the top of the block are
  byte-for-byte unchanged for Training's sake)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)

No `components/Seo/*.js`, `pages/services/seo/index.js`, or Training/DM files touched.

## UI Implementer — DM page: black-accent-to-navy follow-up (color only)
Re-audited the `.dm-*` block for literal black/near-black used as a deliberate accent (badges,
dark surfaces, headings, buttons), per the user's request to match AppDev's navy `#0a1f44`
instead. Found the pricing "most popular" tone (`.dm-plan.is-popular` → `$dm-navy`) was already
`#0a1f44` from the prior DM pass — no change needed there.

Found one real miss: `$dm-dark` (the `.dm-dark` section-band variant, live on `Channels.js`,
`CtaBand.js`, `Results.js`) was `#0b0b0d`, sourced from a comment claiming that's AppDev's
`.app-platforms`/`.app-hero-media` dark-band color. Checked the live `.app-*` block directly
(lines ~7687, 7781) — both are actually `background-color: #0a1f44` (navy) today, not `#0b0b0d`.
That `#0b0b0d` figure appears to predate an AppDev revision; the comment/variable was never
updated. Changed `$dm-dark` from `#0b0b0d` to `#0a1f44` to match AppDev's current real value.

Left `.dm-campaign-badge`'s `#111318` alone — cross-checked against `.app-fv-bubble`
(line ~8555), which is still genuinely `#111318` in the live AppDev block (not navy), so DM
already matches exactly there; forcing it to navy would have made DM diverge from AppDev, not
converge.

**Flag, not fixed**: the SEO page's `$seopal-black` (`styles/style.scss` ~L9860) carries the
same stale `#0b0b0d` assumption about `.app-platforms`/`.app-hero-media`, set in the SEO
color-palette pass logged above. Same live-AppDev mismatch applies there but SEO is out of this
task's scope (DM only) — flagging for whoever next touches SEO's palette.

### Verified
- `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — compiled clean.
- Compiled CSS: `.dm-section.dm-dark { background: #0a1f44; }` confirmed at the correct
  selector. Grepped the `.dm-*` block source for `black`/`#000` — zero remaining hits.

### Files touched
- `Web/Backup/ar/styles/style.scss` (one variable value + comment, `.dm-*` block only)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)

## UI Implementer — SEO CaseStudies + Reporting redesign from user reference screenshots
Task: redesign two SEO sections toward `public/images/inspiration/seopage-insp.png` (results
split) and `seopage-insp2.png` (progress rings). Both refs read first via the Read tool.

**CaseStudies.js**: dropped the old `.seo-dark`/`.seo-card-dark` 3-card grid for a light
two-column `.seo-split` (this page's user rejected dark cards outright in an earlier pass —
`.seo-card-dark` is already white-on-navy per the comments around style.scss:10398, so a
`seo-alt` light section is consistent with that direction, not a reversal of it). Left column:
new `.seo-badge-solid` filled pill, `seo-h2`, `seo-p`, and the "مطلوب من العميل" flag carried
over (still no real case-study numbers). Right column: new `.seo-metric-grid`, 2x2
`.seo-metric-card` tiles — trend icon (`bx-trending-up`/`bx-trending-down`, both confirmed
present in `public/fonts/boxicons.svg`) top-left, a pending-shimmer value (reusing the
`.seo-stat-pending` shimmer language under a new `.seo-metric-pending` class) instead of a
fabricated number, a "قريبًا" chip standing in for the reference's percent-change tag, and a
real metric label (organic traffic growth, keywords in top results, leads from search, avg.
keyword position). No fake numbers shipped — same "coming soon, not broken" rule the file
already had.

**Reporting.js**: replaced the animated bar-chart mock (`.seo-report-bars`) with a row of 4
SVG partial-circle rings (`.seo-ring-row` / `.seo-ring`), one per report metric (rankings,
organic traffic, backlinks, conversions), each a distinct accent color. Ring stroke animates via
framer-motion (`strokeDashoffset`, `whileInView`, reduced-motion safe) — kept inside the
existing `.seo-media-panel`/`.seo-report-card` card shell, only the interior changed. Ring
values are illustrative mock data, same convention the old bar heights already used (not a real
client report).

New SCSS appended to `styles/style.scss` under `// === Seo: CaseStudies (results split) +
Reporting (ring row) ===`, right before the Digital Marketing block (kept adjacent to the rest
of the `.seo-*` system rather than tacked onto the file's end). Reused `$seopal-*` tokens and
the `.seo-card` soft-emboss shadow recipe throughout — no new shadow/radius language. Left
`.seo-report-bars`/`.seo-report-bar`/`.seo-report-foot` classes in place (unused now, but not
asked to remove and other files could reference them — flag if a cleanup pass wants them gone).

Recompiled: `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — clean.
Verified in-browser: killed a stray dev server left over from a botched first background start
(EADDRINUSE + a corrupted `.next` manifest from clearing cache mid-boot), `rm -rf .next`, fresh
`npm run dev`, `GET /services/seo/` → 200, confirmed `seo-metric-grid`/`seo-ring-row`/
`seo-badge-solid`/`seo-split-results` all present in the rendered HTML, no compile errors.

Added both components to `brain/components/REGISTRY.md`.

### Files touched
- `Web/Backup/ar/components/Seo/CaseStudies.js` (rewritten)
- `Web/Backup/ar/components/Seo/Reporting.js` (rewritten)
- `Web/Backup/ar/styles/style.scss` (new block, `.seo-*` — see above)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)
- `brain/components/REGISTRY.md` (two new rows)

## UI Implementer — SEO Pillars split + Reporting ring-row restructure (follow-up round)

Held `brain/locks/styles-style-scss.lock.md` for the duration, released at the end.

**`Seo/Pillars.js`** — rebuilt from the dark 3x2 card grid to the light split layout in
`public/images/inspiration/seopage-insp.png`: left column gets `.seo-badge-solid` ("المحاور") +
existing `.seo-h2`/`.seo-p`; right column is a `.seo-grid.seo-grid-2` (2 cols, added `.seo-grid-2`
modifier to the shared `.seo-grid` rule) of `.seo-card.seo-card-corner` cards — icon-well (now
`.seo-card-corner` variant, 40px) + title + text, all 6 pillars' copy unchanged. Section switched
from `.seo-dark` to `.seo-alt` (light gray) to keep the page's light/dark alternation (Results →
Pillars → AiSearch would've been three whites in a row otherwise).

**`Seo/Reporting.js`** — dropped the `.seo-split`/`.seo-media-panel`/`.seo-report-card` two-column
mock (rings boxed in a half-width panel opposite the text). New layout: text block (heading +
paragraph + checklist) full width on top, then a full-width `.seo-ring-row.seo-ring-row-full` below
with all 4 rings — label above each ring (`.seo-ring-label`, was below), value now centered inside
the ring via `.seo-ring-circle` (absolute-positioned `.seo-ring-value`) instead of sitting under it.
Ring SVG/stroke-draw animation and the 4 metrics (ترتيب الكلمات / الزيارات العضوية / الروابط الخلفية
/ التحويلات) untouched. Dropped the `.seo-skel-badge` "تقرير الأداء الشهري" chip that belonged to the
old boxed mock — not part of the kept ring content. `.seo-media-panel`/`.seo-skel-badge` SCSS rules
left alone (still used by `Hero.js`/`AiSearch.js`); `.seo-report-card` rule is now unused by any
component but wasn't one of the three flagged for removal, left in place.

Removed the three dead rules flagged from the earlier round: `.seo-report-bars`, `.seo-report-bar`
(+ its now-unused `@keyframes seoBarGrow`), `.seo-report-foot`. Dropped `.seo-report-bar` from the
`prefers-reduced-motion` selector list too (target no longer exists).

Recompiled: `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — clean, no
errors. Verified in-browser: port 3000 and 3099 were both occupied by other processes already (3099
served a transient 500 mid-recompile — not mine, left it alone), started a scratch dev server on
`port=3055`, `GET /services/seo/` → 200, confirmed `seo-split-pillars`/`seo-grid-2`/`seo-card-corner`/
`seo-ring-row-full`/`seo-ring-circle`/"المحاور" all present in the rendered HTML, no compile errors.
Killed the scratch server (PID on :3055) afterward — did not touch the pre-existing 3000/3099 processes.

Added both components' rows to `brain/components/REGISTRY.md` (Reporting row updated in place,
Pillars row added).

### Files touched
- `Web/Backup/ar/components/Seo/Pillars.js` (rewritten)
- `Web/Backup/ar/components/Seo/Reporting.js` (rewritten again — layout only)
- `Web/Backup/ar/styles/style.scss` (`.seo-grid-2` added to `.seo-grid`; new `// === Seo: Pillars
  (split layout) ===` block appended at EOF; `.seo-ring-row`/`.seo-ring`/`.seo-ring-value`/
  `.seo-ring-label` edited in place for the new full-width label-over-ring layout; dead
  `.seo-report-bars`/`.seo-report-bar`/`@keyframes seoBarGrow`/`.seo-report-foot` removed)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)
- `brain/components/REGISTRY.md` (Pillars row added, Reporting row description updated)

## UI Implementer — SEO page: Pillars restacked, Reporting rings all-good
Task 1: `components/Seo/Pillars.js` — dropped the left/right split
(`.seo-split-pillars` + `.seo-grid-2`) for a stacked/centered layout matching
the rest of the page (`.seo-head.seo-center`, same as Process.js/Results.js):
badge + heading + paragraph centered full width on top, all 6 pillar cards
below in the shared 3-col `.seo-grid` (collapses 3→2→1). Card markup
(`.seo-card-corner`, icon well/title/text) unchanged. Grepped
`components/Seo/**` — `.seo-split-pillars` had no other user, so removed the
rule from `styles/style.scss`; `.seo-grid-2` also now unused anywhere but
left in place (comment updated) in case a future split grid needs it.

Task 2: `components/Seo/Reporting.js` — ring values bumped into a
consistently strong range so the section reads as proof the service works:
ترتيب الكلمات 72→88, الزيارات العضوية 85→91, الروابط الخلفية 64→79,
التحويلات 58→76. Added a small positive state tag per ring
(`.seo-ring-state`: ممتاز / نمو قوي / جيد جدًا / جيد جدًا) between the label
and the circle. Ring markup/animation mechanics untouched.

Files: `components/Seo/Pillars.js`, `components/Seo/Reporting.js`,
`styles/style.scss` (removed `.seo-split-pillars` rule, updated `.seo-grid-2`
comment, added `.seo-ring-state`, tightened `.seo-ring-label` margin),
`styles/style.css` + `.css.map` (recompiled).

Verified on scratch port 3055: `/services/seo/` compiles clean (784 modules,
no errors besides the pre-existing caniuse-lite/viewport warnings), both
sections render (6 `.seo-card-corner`, 4 `.seo-ring-state`, 1 `.seo-badge-solid`
in Pillars). Server killed after check. No lock left open.

## UI Animator — SEO page hover/interaction pass
Audited every `components/Seo/*.js` section: cards, icon wells and buttons
already had hover states from the visual reskin (`.seo-card`/`.seo-icon-well`
lift + color deepen, `.seo-btn*` brightness/shadow), but several restructured
elements had none — `.seo-ring` (Reporting), `.seo-list li` check items,
`.seo-accordion .accordion__button` (FAQ), `.seo-metric-icon` (CaseStudies),
`.seo-plan-badge`. Added one CSS-only hover pass, appended after the existing
`.seo-card-corner` rule (`styles/style.scss`, new `// === Seo:
hover/interaction pass (UI Animator) ===` block, no other rule touched):
- `.seo-icon-well` on card hover now nudges (translateY + rotate), same
  transform AppDev's own `.app-feature-card:hover .app-feature-icon` uses —
  brings Pillars' cards onto the site's existing icon-hover language instead
  of only recoloring.
- `.seo-metric-card:hover .seo-metric-icon` gets the same nudge + a touch of
  scale so the trend chevron is what pops on the CaseStudies tiles.
- `.seo-list li:hover` (AiSearch differentiators, Reporting report contents,
  Pricing features) — icon scales + brightens to `$main-color`, text
  deepens to full ink. Vertical-only, no horizontal translate → RTL-safe.
  Popular pricing card gets its own hover-text override so it stays white on
  navy instead of picking up the light-card ink color.
- `.seo-accordion .accordion__button:hover` — soft background wash + the
  sign well deepens to the same tint it already gets on expand, so hover
  previews the click instead of doing nothing.
- `.seo-ring:hover` (Reporting) — ring card gets a light wash, the SVG arc
  scales + gets a soft drop-shadow, the center value and the state tag
  (`ممتاز`/`نمو قوي`/etc.) both emphasize. Didn't re-trigger the
  strokeDashoffset draw-in — that's a one-shot scroll reveal already gated by
  `useReducedMotion`, and restarting a "loop" on every hover pass would break
  the Lab's no-orphaned-tween rule.
- `.seo-plan.is-popular:hover .seo-plan-badge` — small scale pulse so the
  "الأكثر طلبًا" badge moves with the card instead of riding along static.

None of this needed a reduced-motion guard: every new rule is a hover-only
CSS transition (transform/color/background), same convention already used
unguarded by `.seo-card`/`.app-feature-card` hover on this page — only
infinite/looping keyframe animations get the `@media
(prefers-reduced-motion: reduce)` block here, and nothing new loops.

Left alone deliberately: `.seo-badge-solid`, `.seo-eyebrow`, `.seo-plan-price`
— static, non-interactive labels with no click target, so a hover state on
them would be motion for its own sake. `.seo-btn`/`.seo-btn-ghost`/
`.seo-btn-invert` and the `Magnetic`-wrapped CTAs were already covered by the
earlier visual pass and untouched.

Files: `styles/style.scss` (append-only, one new block after
`.seo-card-corner`), `styles/style.css` + `.css.map` (recompiled,
`--style=expanded`). No component `.js` files touched — this was pure CSS,
targeting classes already emitted by the current markup.

Verified on scratch port 3099 (`.next` was stale from a prior/concurrent run
and threw a manifest `JSON.parse` 500 on first boot — cleared `.next` and
recompiled clean, 784 modules, no errors besides the pre-existing
caniuse-lite/viewport warnings). Confirmed `seo-ring`, `seo-metric-card`,
`seo-card-corner`, `seo-icon-well`, `seo-accordion` all present in the SSR
HTML. Server killed after check, `styles-style-scss.lock.md` removed.

## UI Implementer — Training page (T-011) reskinned to AppDev's visual system
Task: bring the `.tr-*` SCSS block (`styles/style.scss`, in-place at ~L12160-13097) in line with
`components/AppDev/**`, the same way `.seo-*`/`.dm-*` were reskinned earlier today — the last of
the three G1 service pages.

Read `components/AppDev/Hero.js`/`Features.js` + their `.app-*` CSS and compared against all 12
`components/Training/*.js` files and the existing `.tr-*` block, which was still riding on the
`$seo-*` tokens (`$seo-black`/`$seo-white`/`$seo-gray`/`$seo-ink`/`$seo-ink-dim`/`$seo-line*`/
`$seo-card-dark`) rather than its own — the exact coupling flagged in an earlier SEO-palette log
entry today (`$seo-*` reuse by Training). Followed the DM reskin's pattern instead: new `$tr-*`
token set holding AppDev's literal values, scoped to the Training block only, `$seo-*` left
byte-for-byte alone.

### Values pulled from AppDev (not approximated), same recipe as the DM pass
- `$tr-line`/`$tr-line-dark`/`$tr-ink`(`$heading-color`)/`$tr-ink-dim`(`$body-color`)/
  `$tr-surface-alt`(#f5f6f8)/`$tr-navy`(#0a1f44)/`$tr-radius`(16px)/`$tr-radius-sm`(10px).
- `.tr-section.tr-dark` background: `$seo-black` (#1b1d23) → `$tr-navy` (#0a1f44, AppDev's actual
  dark-band color per the `$dm-dark` correction logged earlier today).
- `.tr-card`/`.tr-alt .tr-card`: dropped the translateY(-5px) hover-lift + border-color-darken,
  now `.app-feature-card`'s exact recipe (flat white, 16px radius, hairline border, shadow-only
  hover). Formats/LearningPath/Instructors/Outcomes/Catalog all inherit this via `.tr-card`.
- `.tr-card-dark` (Instructors, on `.tr-dark`): was `$seo-card-dark` (#24262d, a literal dark
  card) — matched the SEO/DM "user hates black cards" fix already applied twice today: now a
  plain white card with AppDev's full soft-emboss shadow (`.app-plan-card` recipe), floating on
  the navy section instead of a same-family dark tone. `.tr-h3`/`.tr-p`/`.tr-stat-label` inside
  win their dark ink back since the card itself is white now.
- `.tr-icon-well`: 46px/13px-radius neutral grey → `.app-feature-icon`'s exact 40px/10px/`#eef7fb`
  bg/cyan-at-rest (was cyan-on-hover-only).
- `.tr-avatar-circle` (Instructors/Outcomes): flat grey placeholder circle → cyan-tinted
  (`rgba(29,211,248,.12)` bg, `$main-color` icon) to match the icon-well language instead of
  sitting outside the palette; `.tr-avatar-sm` (Outcomes) → `$tr-surface-alt` bg.
- `.tr-btn`: full pill (100px) + black fill → `.default-btn`'s 10px radius + `$main-color` fill,
  shadow re-keyed from near-black to cyan on hover. `.tr-btn-ghost`: 1.5px `$seo-line` border →
  `.app-hero-ghost-btn`'s 1px `#d9dde3` + cyan hover.
- `.tr-list li i` (Certificate/Pricing): plain colored `bx-check` → `.app-plan-features li i`'s
  20px cyan-tinted circle badge (same swap DM's list already got).
- `.tr-plan`/`.tr-plan-badge`: flat-bordered card + soft-cyan-pill badge → `.app-plan-card`'s
  soft-emboss shadow + solid-cyan-fill badge; `.is-popular` navy `$tr-navy` (was `$seo-black`).
- `.tr-accordion .accordion__item` (Faq): 1px border → `.app-faq-accordion`'s box-shadow-only
  chrome, sign circle cyan-tinted at rest (was neutral-grey-until-expanded).
- `.tr-frame-card`/`.tr-cert-card` (Hero/Certificate device mocks): radius 26px/18px → `$tr-radius`
  (16px), flat drop-shadow → the same big soft-emboss recipe as `.app-hero-frame`/
  `.dm-campaign-card`.
- Headings (`.tr-h1/h2/h3`) font-weight 800 → 700 (AppDev headings are all 700); `.tr-step-num`
  800 → 700.

### One structural correction, not a pure token swap
`.tr-cta-band .tr-h2 { color: white }` was the direct copy of DM's equivalent rule — but DM's
`CtaBand.js` renders on `.dm-section.dm-dark` while **Training's `CtaBand.js` renders on
`.tr-section.tr-alt`** (light gray), an existing mismatch in the component, not something I
introduced. Copying the white-text rule verbatim would have made the CTA heading invisible on its
actual light background. Did not change `CtaBand.js`'s section variant (that's a UX/rhythm call,
not mine to make unasked) — instead left `.tr-cta-band .tr-h2` using the default dark-ink color,
which is correct for the background it's actually placed on, and left a comment explaining why
it diverges from the DM pattern.

### Icon fix, in scope per the shared `bxs-*` gotcha
Found two `bxs-*` (solid) icons that render as empty boxes per the documented gotcha:
`Outcomes.js`'s quote icon (`bxs-quote-alt-left` → `bx-quote-alt-left`) and `Certificate.js`'s
seal icon (`bxs-award` → `bx-medal`, no regular `bx-award` glyph exists in the bundled set).

### Verified
- `npx sass styles/style.scss styles/style.css --style=expanded --source-map` — compiled clean,
  0 errors.
- Grepped the rewritten Training block for leftover `$seo-*` references — zero; all now `$tr-*`
  or shared tokens (`$main-color`/`$white-color`/`$heading-color`/`$body-color`/`$transition`).
- Read all 12 `components/Training/*.js` files to confirm every class the JSX references exists
  in the rewritten block (no orphaned classnames, no missing ones) — clean.
- Live: scratch dev server on `port=3099` (killed the prior session's process on that port first
  via PID match on `server.js`), `GET /training/` → 200, `dir="rtl"` present in the SSR HTML,
  `tr-card`/`tr-btn` classes present, no console/compile errors beside the pre-existing
  caniuse-lite/viewport warnings. Killed the scratch server afterward (verified via a failed
  curl — connection refused).
- Held `brain/locks/styles-style-scss.lock.md` for the duration, releasing now.

With this landing, **all three G1 service pages (SEO/DM/Training) share one visual system** —
T-011 moved to REVIEW. T-017 (delete `styles/ux-prototype.css` + its `_app.js` import) can run
once the user has looked at all three.

### Files touched
- `Web/Backup/ar/styles/style.scss` (`.tr-*` block rewritten in place, ~L12160-13097, no other
  banner touched)
- `Web/Backup/ar/styles/style.css`, `Web/Backup/ar/styles/style.css.map` (recompiled)
- `Web/Backup/ar/components/Training/Outcomes.js` (icon fix: `bxs-quote-alt-left` → `bx-quote-alt-left`)
- `Web/Backup/ar/components/Training/Certificate.js` (icon fix: `bxs-award` → `bx-medal`)
- `brain/MANAGEMENT.md` (T-011 row → REVIEW, Work Assigned row added then released)
