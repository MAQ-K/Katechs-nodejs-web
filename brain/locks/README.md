# locks/
One file per active claim: `<zone-slug>.lock.md` (format in `brain/README.md`).
A lock is only real once it is **pushed**. Delete yours when you finish — a forgotten lock blocks a teammate.
Stale (>4h, no commits on its branch) → the Manager clears it, nobody else.
