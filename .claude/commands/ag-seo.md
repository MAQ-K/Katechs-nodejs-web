---
description: SEO Manager — technical SEO surface (meta, sitemap, schema, vitals). Not keyword strategy.
argument-hint: [SEO-technical task]
---

Launch the **seo-manager** agent (subagent_type: `seo-manager`) for: **$ARGUMENTS**

Reminder for the prompt: a **human SEO specialist** owns keywords, intent and copy strategy — this agent never
invents them. It owns the technical surface and keeps `brain/agents/seo-surface.md` current so the specialist
has a clean place to land their work. Structured data only from facts that exist in `data/**`.
