# Katechs (AR) — Project Management File

> **ARCHIVED — 2026-08-24.** This file is no longer the source of truth. It is kept as the pre-brain
> history archive. All current context, tasks, assignments and history now live in `brain/` at the repo root:
> start with `brain/MANAGEMENT.md`, then `brain/STATE.md`, then `brain/README.md` (the working protocol).
> **Do not write new entries here.** New history goes to `brain/logs/<date>.md`.

> Read this file first in any new chat about this project. Keep it updated as work progresses — it's the single source of truth for context across sessions.

## Brief

`Web/Backup/ar` is the **Arabic** build of the Katechs website — a Next.js 14 (Pages Router) IT/hosting/security services site, built on the "Jumpx" React/Next.js template. Bootstrap 5 + custom SCSS theme, RTL layout (`styles/rtl.css`), AOS scroll animations, Swiper carousels.

Current goal: **build new pages** using the **same theme/design system** (colors, fonts, Navbar/Footer, component conventions) but with a **different UI/UX style** for the new page content.

## Environment

- **Framework**: Next.js 14, Pages Router (not App Router). Custom `server.js` (plain `http` server wrapping Next, not `next start`).
- **Styling**: Bootstrap 5 + `styles/style.scss` / `styles/responsive.scss` (compiled to `.css` + `.css.map`, committed). `styles/rtl.css` handles RTL. No CSS Modules / styled-components — global SCSS classes.
- **Structure convention**:
  - `pages/<route>.js` — thin page files, compose `Layout` + a stack of components.
  - `components/<PageName>/` — components dedicated to one page (e.g. `HomeOne`, `PricingWebsite`, `securityDetails`).
  - `components/Common/` — shared cross-page sections (CTA, Testimonials, Team, PageBanner, Newsletter, Partner, etc.).
  - `components/Layouts/` — `Navbar.js`, `Footer.js` (the shared shell).
  - `components/Shared/GoTop.js` — shared utility widget.
  - `utils/baseUrl.js` — env-aware base URL (`Katechs.com` in prod, `localhost:3000` in dev).
- **Forms/email**: `pages/api/contact.js` and `pages/api/web.js` send mail via `nodemailer-sendgrid-transport`. reCAPTCHA is client-side only (not verified server-side — see Known Issues).
- **Run locally**: `npm run dev` (runs `node server.js`, port 3000). `npm run build` for production build.
- **Lint**: `eslint:recommended` + `plugin:react/recommended`, but `next.config.js` sets `eslint.ignoreDuringBuilds: true` — lint does not block builds.

## Design References

- **Motion / 3D / UI Kit Lab** — https://claude.ai/code/artifact/ebce0bd4-72de-4487-aa0c-43dcab93d7bb — a live, interactive reference (not a mockup — every demo actually runs) covering: three animation tiers (Framer Motion reveals, GSAP ScrollTrigger, ambient/3D) with install + wiring code for this specific Next.js Pages Router + RTL setup; six advanced 3D scenes in raw WebGL/canvas with a React Three Fiber pipeline guide; six 21st.dev components assessed and ported to vanilla CSS (this project has no Tailwind/shadcn — see verdicts before reusing any 21st.dev/shadcn snippet); and a UI Kit section with the exact soft-emboss shadow recipe (4-layer box-shadow, not a flat blur), button/badge/card/typography patterns pulled from a reference template the user liked. **Before styling any new card, button, badge, or adding motion/3D to a page, check this lab first** — it documents what fits this codebase and what doesn't, and why.

## History

- **2026-08-18** — Did a full review of `Web/Backup/ar`. Key findings (see Known Issues below): hardcoded SendGrid API key in two API route files, broken `.gitignore` entry for `sendgrid.env`, 32 `npm audit` vulnerabilities (incl. critical in `swiper`), reCAPTCHA not verified server-side, no input sanitization on contact forms, stray files committed (`contact.zip`, log files, `check-mockup.png`).
- **2026-08-18** — Agreed direction: build new pages reusing the existing theme/shell (Navbar, Footer, SCSS tokens) but with a distinct UI/UX treatment for new page content, following the existing `pages/<route>.js` + `components/<PageName>/` pattern.

## Known Issues / Security Debt (not yet fixed — do not ignore)

1. **[Critical]** Hardcoded live SendGrid API key in `pages/api/contact.js:7` and `pages/api/web.js:7`. Needs rotation + move to `process.env.SENDGRID_API_KEY`.
2. **[Critical]** `.gitignore` has a corrupted line meant to ignore `sendgrid.env` (currently untracked but NOT actually ignored — a plain `git add .` would stage a file containing a raw API key).
3. **[Critical]** `npm audit`: 32 vulnerabilities (9 critical, 14 high), notably prototype pollution in `swiper` (actively used for carousels).
4. **[Medium]** reCAPTCHA only gates the submit button client-side; `/api/contact` and `/api/web` never verify the token server-side — bypassable by calling the API directly.
5. **[Medium]** No input validation/escaping before interpolating form fields into email HTML (`pages/api/contact.js`, `pages/api/web.js`) — HTML injection into support inbox possible.
6. **[Low]** Committed log/junk files: `stderr.log` (leaks prod server path), `dev-stdout.log`, `dev-stderr.log`, `pages/contact.zip`, `check-mockup.png`, `tmp/restart.txt`.

## Plan

- [ ] Fix credential exposure (rotate key, env var, `.gitignore` fix) — should happen before any further commits/pushes.
- [ ] Define the new UI/UX direction for new pages (style reference, layout ideas) — pending input.
- [ ] Build new page(s): page file in `pages/`, dedicated component folder in `components/`, reuse `Layout`/Navbar/Footer, reuse SCSS tokens, new visual treatment for the content sections.
- [ ] (Optional/parked) Address `npm audit` findings, server-side recaptcha verification, input sanitization.

## Task Log

| Date | Task | Status |
|------|------|--------|
| 2026-08-18 | Full codebase review | Done |
| 2026-08-18 | Create this management file | Done |
| 2026-08-18 | Hosting services — Stage 1 (Understand): wrote `data/hosting-services/brief.md` + `data.js`, scaffolded flat data folders for vps-hosting/wordprees-hosting | Done — pending user review |
| 2026-08-18 | Hosting services — Stage 3, Section 1 (Hero): built `components/HostingServices/Hero.js`, new `.hosting-hero*` styles in `styles/style.scss` (compiled to `style.css`), wired into `pages/services/hosting-services/index.js` replacing `PageBanner`, added `#pricing` anchor. Verified rendering via dev server. Placeholder image (`main-img1.png`) — real asset pending. | Done — pending visual review with user |
| 2026-08-18 | Hero refinement: real hero image (`public/images/hosting-services/hero-website-live.png`, user-provided) wired in; heading/eyebrow/badge switched to Cairo font (already loaded in `_document.js`, previously unused — scoped to hero only, sitewide `$heading-font-family` untouched); image capped to 420px; domain floating card redesigned as a pill (globe icon + domain text) + separate shield-icon badge. | Done — pending visual review with user |
| 2026-08-18 | Hero: domain badge reworked (rounded-card not full pill, deeper shadow, cursor accent) + image bumped to 520px after "shitty badge" feedback. Added second floating card — rocket icon + conic-gradient % ring at bottom-right of hero image. | Done — pending visual review with user |
| 2026-08-18 | Hero: reverted the rocket/speed-card addition per user correction (misread the ask). Attempted full-width CTA bar per re-annotated sketch — user rejected it, reverted back to compact `default-btn` inside the text column. | Reverted |
| 2026-08-18 | Hero: added rocket loader bar — full-width animated progress track at the bottom of the hero section (below the text/image row), fills to 99% via CSS `@keyframes` + custom property, rocket icon riding the leading edge, label + percentage below. Confirmed placement with user via AskUserQuestion before building (learned from prior back-and-forth). | Done — pending visual review with user |
| 2026-08-18 | Hero: added a small continuous float animation on the loader rocket, then reverted immediately per user ("REVERT") — rocket is back to static positioning at the fill's leading edge, no ongoing animation. | Reverted |
| 2026-08-18 | Hero rebuilt against a full-page reference screenshot the user provided (`public/images/New folder/chrome-capture-2026-08-18.png`, a Hostinger AR hosting landing page). Removed the rocket loader bar (not in the reference), added a circular "speed ring" badge (99, lightning icon) at the image's bottom-right mirroring the reference's PageSpeed card, and added a 5-star "ممتاز" trust row under the CTA button (no fabricated third-party review logos). User confirmed via AskUserQuestion: continue section-by-section workflow; pricing cards up next. | Done — pending visual review with user |
| 2026-08-18 | Hero: swapped column order — image column now first in DOM (renders right in RTL), text column second (renders left), matching user's "image right, text left" request. No SCSS changes needed. | Done |
| 2026-08-18 | Pricing section modernized: new `components/HostingServices/Pricing.js` (first component built directly against `data/hosting-services/data.js`'s `pricing` object — no more hardcoded plan JSX), replaces `PricingShareHosting/PricingStyleOne` in the page. New `.hosting-pricing-*` SCSS classes (not touching shared `.single-pricing`/`.currency-switch` used by other pricing pages) — modern cards, middle "الاكثر طلبا" plan highlighted in solid black + scaled up, pill currency switch, checklist with check icons. | Done — pending visual review with user |
| 2026-08-18 | Fixed: `bx bxs-check-circle` (a "solid" Boxicons variant) rendered as an empty box in the pricing checklist — the CSS rule/codepoint exists in `styles/boxicons.min.css` but the bundled icon font lacks that glyph. Swapped to plain `bx bx-check` (confirmed working elsewhere) wrapped in a small circle badge for the same visual effect. **Gotcha for future icon choices: prefer `bx-*` (regular) over `bxs-*` (solid) in this project unless verified to render.** | Fixed |
| 2026-08-18 | Fixed unrelated pre-existing bug (spotted via user screenshot, not part of hosting-services scope): `components/Common/Partner.js` (used on `/` and `/about-us/`) had zero CSS for `.logo-section`/`.logo-container`/`.logo-box` — raw `<img>` logos (up to 2560×742px, e.g. Laravel) rendered at native size, huge and overlapping. Added proper flex layout + normalized logo height (45px) in `styles/style.scss`. | Fixed |
| 2026-08-18 | Remade the "جرب لوحة التحكم cPanel" FAQ section into a banner: new `components/HostingServices/CpanelBanner.js` + `cpanelBanner` data in `data/hosting-services/data.js`, replaces `FaqContent` on this page only (`FaqContent` itself untouched — still used by vps-hosting/wordprees-hosting). Layout: text+CTA on the right (`default-btn`, same theme as elsewhere), illustration (`public/images/faq-img.png`, existing asset, blue palette) overflowing the top of a rounded gradient-tinted banner frame on the left, per user's sketch + reference image. Confirmed left/right placement with user via AskUserQuestion before building (sketch and written instructions conflicted). | Done — pending visual review with user |
| 2026-08-18 | Noticed (not by me) `pages/services/hosting-services/index.js` changed on disk: `ServicesStyleTwo` commented out, `FeatureHost` swapped for a new `components/HostingServices/FeatureGrid.js`. Left as-is per instructions (current state, not reverted). | Noted |
| 2026-08-18 | Pricing: changed the popular plan card ("استضافة الموزعين") background from solid black to dark navy blue (`#0a1f44`) per user request. | Done |
| 2026-08-18 | Fixed broken footer logo/shape images (user screenshot). Root cause: corrupted `.next/cache` (webpack "invalid block type" errors seen a few steps earlier when restarting the dev server). Image files themselves were always valid (verified with `sharp`). Cleared `.next/cache` and restarted — all footer images now resolve 200. | Fixed |
| 2026-08-18 | Feature grid ("التزويد الفوري" card, `components/HostingServices/FeatureGrid.js` — new component that appeared on disk outside this session) had no explicit font-family on its `h3`/`p`, inheriting an inconsistent fallback. Set `h3` to Cairo (matches every other new-section heading: Hero, Pricing, CpanelBanner) and `p` to `$body-font-family` explicitly. | Fixed |
| 2026-08-18 | cPanel banner: swapped the placeholder illustration for the user's new SEO/growth-themed image (moved to `public/images/hosting-services/cpanel-banner-seo.png`, was in `public/images/New folder/`), updated `next/image` dimensions for its landscape aspect ratio (1536×1024 → 460×307) and reduced the overflow pull-up (-100px → -70px) to match the shorter height. **Flag:** this image is a photo composite with its own blurred/vignette edges, unlike the old transparent illustration — worth a visual check that it blends well with the gradient banner frame once viewed. | Done — pending visual review with user |
| 2026-08-18 | cPanel banner: made image bigger — widened its column from col-lg-5 to col-lg-6 (text column narrowed 7→6 to match) and bumped `next/image` size 460×307 → 690×460, same 3:2 ratio. | Done |
| 2026-08-18 | cPanel banner: image now visually breaks out past the banner frame (bigger than the banner, not just bigger overall) — `width: 135%` on the img (overriding `max-width`), pulled up `-140px` and left `-70px` so it overflows the card's top and outer edge, plus a stronger drop shadow to sell the "standing out" effect. Reset to contained 100%-width on mobile (<992px). Bumped intrinsic `next/image` size to 780×520 for quality at the larger display size. | Done — pending visual review with user |
| 2026-08-18 | cPanel banner image: per user's exact style spec, dialed width down 135%→110% and dropped the drop-shadow (commented out, not deleted). | Done |
| 2026-08-19 | **Emails page — structure pass.** Built `components/Emails/` (`Hero`, `PricingFlow`, `FeaturesPlaceholder`, `FaqPlaceholder`, `wireframeStyles.js`) as gray-box wireframes with real interaction only, wired into `pages/services/emails/index.js` (replaced `PageBanner`/`ServiceDetailsContent`/`Email_typs`/`PricingEmail`/`FaqContent`). Layout from user's sketch: hero (image + title/paragraph/3 bullets), 3 big option cards → synced compact tab bar → plan cards that swap per type, then Features and FAQ blocks. Confirmed interaction model with user before building. | Done |
| 2026-08-19 | Emails hero UI (section 1): `.email-hero` — soft-emboss gradient image card (recipe from the Motion/3D/UI Kit Lab, retoned to `$main-color` cyan), Cairo headline, 3 icon-badge bullets, `default-btn` CTA. Per user: 160px top padding to clear the navbar, card centered in its column, **16px radius (from the reference image, not the lab's 40px)**, image column left / text right. Placeholder envelope icon — no suitable real photo in `public/images` yet. | Done |
| 2026-08-19 | Emails option cards (section 2): `.email-type-card` — logo tile, real copy reused from `Email_typs.js`, active state = cyan gradient + "اختر هذه الباقة", hover lift. Per user: card radius 5px, icon tile 8px radius and no shadow. | Done |
| 2026-08-19 | Emails pricing (section 3): created `data/emails/data.js` (all plans/prices/features carried over verbatim from `components/PricingEmail/*`), rebuilt tab bar (pill tabs, cyan active) + `.email-plan-card` (soft-emboss, check/x feature list, navy `#0a1f44` most-popular card matching the hosting page). **Gotcha: named the modifier `is-popular`, not `popular` — the theme's global `.popular` (style.scss:3325) is a rotated red corner ribbon and was rotating the whole card 45°.** | Done — pending visual review with user |
| 2026-08-19 | Emails features (section 4) + FAQ (section 5). Features: bento grid (`.email-features-grid`, CSS Grid 6-col, 3 cards span-2 + 2 cards span-3) per user's reference screenshot — flat white cards, thin border, no emboss shadow, deliberately different from this page's other sections. All 5 feature claims restate facts already stated elsewhere on this page (99.9% uptime, antivirus/spam, cross-device, migration tool) — none invented. FAQ: real `Accordion` from `react-accessible-accordion` (already a dependency, used elsewhere), 5 new Q&As grounded in existing page facts — flagged in `data.js` for the client to review wording since the source FAQ components (`EmailFag` etc.) are single blurbs, not real Q&A pairs to copy from. | Done — pending visual review with user |
| 2026-08-19 | **Motion layer added across the whole page** (framer-motion, added to `package.json`) — landed mid-session from a parallel edit, reconciled rather than reverted per the Design References lab: `components/Common/Reveal.js` (scroll-reveal + stagger helpers, replaces AOS), `components/Emails/Tilt3D.js` (cursor-tilt wrapper on the hero image), `components/Emails/BorderBeam.js` (rotating conic-gradient border on the popular plan card — the lab's top-rated 21st.dev pick, "Take it — best of the six"). I added the missing `.email-plan-beam`/`.email-beam-inner` SCSS (the component had no matching styles yet) and fixed a duplicate FAQ chevron (`react-accessible-accordion/dist/fancy-example.css`, imported globally in `_app.js`, draws its own `::before` arrow that doubled up with the custom plus/x icon — killed via `&::before { display:none }` scoped to `.email-faq-accordion`). **Gotcha: this project's dev server is fragile to concurrent edits — two dev-server instances mid-session (one crashed on file deletion, corrupted `.next` cache produced `SyntaxError: Unexpected end of JSON input` on manifest reads). Fix was killing all node processes on :3000 and deleting `.next` entirely before one clean restart.** All 5 sections verified error-free via Playwright afterward. | Done — pending visual review with user |
| 2026-08-19 | **App Development page — UI design phase started.** User supplied `public/images/app page inspiration/` (4 reference screenshots, one per section: hero, platforms/"dream team" grid, process/"how it works" steps, features cards). Agreed direction: carry the **Emails page theme** (Cairo headings, cyan `$main-color`, soft-emboss card recipe, `default-btn`, 5/16px radii) onto this page while taking **layout** from the inspiration images. Working section-by-section. | In progress |
| 2026-08-19 | App Dev **Section 1 (Hero)** — replaced the gray-box wireframe `components/AppDev/Hero.js` with the real design, from `1st section.png`: dot + slash-separated tag eyebrow, oversized Cairo headline (54px, tight leading), paragraph, **dual CTA** (`default-btn` primary + new `.app-hero-ghost-btn` outlined counterpart — same 10px radius/size, white fill, cyan on hover), and a **double-framed media card** (white outer plate 24px radius → dark inner media 16px radius) with a glassy numbered caption plate ("01") pinned to its bottom. Reused the existing `public/images/mobile-app/app-mockup.png` (KaTechs-branded phone on black, cyan UI accents — matches `$main-color`). Motion: `staggerParent`/`staggerItem` from `Common/Reveal`, `Emails/Tilt3D` on the frame (imported cross-folder rather than duplicated). Added `id="process"` to the Process wireframe section so the ghost CTA anchor resolves. New `.app-hero*` block in `styles/style.scss`. | Done — pending visual review with user |
| 2026-08-19 | **Gotcha: `node` is not on PATH in this environment** (no `C:\Program Files\nodejs`, `npx`/`sass` unusable) even though a dev server is running on :3000. `styles/style.css` had to be updated by hand-writing the compiled equivalent of the new SCSS and splicing it in before the `sourceMappingURL` comment — `.scss` and `.css` are both committed, so **both must be kept in sync manually until node is available again**. | Noted |
| 2026-08-19 | Fixed a **pre-existing** 500 on `/services/app-development/` (`TypeError: getStaticPaths is not a function` from Next's `static-paths-worker`) — no `getStaticPaths` exists anywhere in the repo; it was a corrupted `.next` cache entry for that one route (every other route returned 200). Confirmed the new Hero was not the cause by rendering it on a throwaway route (200), then deleted `.next/server/pages/services/app-development.js` + `.next/static/chunks/pages/services/app-development.js` and touched the page to force a recompile. Route now 200 with the hero markup present. Throwaway route removed. **Same `.next`-fragility class already logged on 2026-08-19 for the Emails page — deleting just the one route's artifacts is a lighter fix than nuking `.next`, and does not require restarting the server.** | Fixed |
| 2026-08-19 | App Dev Hero refinement: hero image made **squarer** per user — `.app-hero-media` given `aspect-ratio: 4 / 5` (was the mockup's native 2/3) with `object-fit: cover` centered, so the phone stays fully in frame while the plate reads less tall; frame `max-width` 460→480px, `next/image` intrinsic 430×645→480×600. | Done — pending visual review |
| 2026-08-19 | App Dev **Section 2 (Platforms)** — replaced the gray-box wireframe `components/AppDev/Platforms.js`, from `2nd secton inspiration just ui.png`: **dark band** (`#0b0b0d`, same tone as the hero's inner media so the two sections read as one system), split header (Cairo 40px white heading on one side, muted paragraph on the other), and 3 portrait media cards (4/5, 12px radius, light cyan-tinted gradient) with the label row sitting **below** the card on the dark ground — title + tech meta on the start side, small "الباقات" arrow link on the end side. Hover lifts the card 8px, tints the glyph cyan, and drops a cyan glow. Scroll-in via `Common/Reveal` with per-card stagger. Content: iOS/Swift, أندرويد/Kotlin, هجينة/Flutter. Added `id="platforms"` here and `id="pricing"` to the Pricing wireframe so the card links resolve. | Done — pending visual review |
| 2026-08-19 | **Decision — platform card visuals:** the inspiration uses 3 distinct photos. The four spare renders in `public/images/mobile-app/` (`417403b7…`, `849e5fb4…`, `984adfac…`, `ffdf755a…`) are all near-identical square laptop+phone composites — they'd read as repetitive and none of them says "iOS" vs "Android". Went with CSS gradient panels + large Boxicons platform glyphs (`bxl-apple`, `bxl-android`, `bx-devices` — all three verified present in `styles/boxicons.min.css`) so the three cards are actually differentiated. **Swap in real per-platform screenshots later if the client supplies them.** | Noted |
| 2026-08-19 | App Dev **Section 3 (Process)** — replaced the gray-box wireframe `components/AppDev/Process.js`, from `process section 3rd section.png`: centered pill badge ("كيف نعمل", `bx-broadcast` in cyan), big centered two-line Cairo heading, and 4 equal-height step cards (`#f5f6f8`, 12px radius) each holding a step label + title + illustration well + white pill tag, with **circular arrow connectors riding the bootstrap gutters** between cards (`inset-inline-end`, so they point along the RTL reading flow; hidden below `lg` where the row wraps). White ground so it alternates against the dark Platforms band above. Steps: التخطيط والدراسة / تصميم UI/UX / التطوير والبرمجة / الإطلاق والدعم. | Done — pending visual review |
| 2026-08-19 | **Decision — step illustrations built in CSS, not images.** The reference's four abstract-UI vignettes were rebuilt from plain divs (`.app-step-plate` skeleton panel + avatar, `.app-step-plate-row` thumb/lines with a cyan check badge, `.app-step-stack` three rotated stacked sheets, `.app-step-bubbles` offset chat bubbles with the middle one inverted to `#111318`). No new assets needed, stays crisp at any size, and each step reads as visually distinct. | Noted |
| 2026-08-19 | **Gotcha: the illustration width helpers `.w-50` and `.w-100` collide by name with Bootstrap 5's width utilities** (`.w-25/.w-50/.w-75/.w-100`, which carry `!important` and therefore win). Harmless *only* because the values are identical (50%/100%). `.w-55/.w-70/.w-80/.w-90` are ours alone. **If these ever need different values, rename them — Bootstrap's `!important` will silently override.** | Noted |
| 2026-08-19 | App Dev **Section 4 (Features)** — new `components/AppDev/Features.js` (replaced `FeaturesPlaceholder.js`, deleted), from `features section.png`: start-aligned two-line Cairo heading, then 3 flat cards whose chrome deliberately matches `.email-feature-card` (white, 1px hairline `rgba(15,23,42,.08)`, 16px radius, no emboss, shadow only on hover) so the two pages agree. Each card ends in a **mini-mockup that bleeds off the card's bottom edge** (card has `overflow:hidden`, mockups carry `margin-bottom:-26px`) — the reference's signature device. Three distinct mockups built from divs: 3 phone frames with the middle one raised + cyan-accented, a code panel with indented rows, and an app-store listing (cyan gradient icon, stars, تثبيت pill) over a dark support bubble. Features: نماذج تفاعلية قبل البرمجة / كود نظيف قابل للتوسّع / نشر على المتاجر ودعم مستمر — all three restate claims the page already makes. | Done — pending visual review |
| 2026-08-19 | App Dev **Section 5 (Pricing)** + **Section 6 (FAQ)** — no inspiration image for these two, so built in the page's established theme. Pricing: eyebrow/heading/note head, 3 equal-height plan cards on the Emails page's soft-emboss recipe, middle plan navy `#0a1f44` + cyan "الأكثر طلبًا" badge (same convention as hosting + emails). FAQ: real `react-accessible-accordion` (6 Q&As) mirroring `.email-faq-accordion`, including the **same `::before { display:none }` fix** for the duplicate chevron that `fancy-example.css` injects globally. Replaced `Pricing.js` (wireframe) and `FaqPlaceholder.js`; `wireframeStyles.js` deleted — **no wireframe files remain in `components/AppDev/`**. | Done — pending visual review |
| 2026-08-19 | **Created `data/app-development/data.js`** (follows the Data Folder Convention; page came from inspiration images, so no `brief.md`). Holds `pricingSection`/`plans` and `faqSection`/`faqs`. | Done |
| 2026-08-19 | **Decision — pricing is quote-based, no numbers invented.** No app-development prices exist anywhere in this codebase, and custom app work is genuinely scoped per project, so every plan shows "حسب نطاق المشروع" with an "اطلب عرض سعر" CTA → `/contactWeb` instead of a fabricated figure. Plans differ on real scope (one platform / both via Flutter / native Swift+Kotlin + dashboard integration), not on price. **If the client supplies real prices, add a `price` field per plan in `data/app-development/data.js` and render it in `components/AppDev/Pricing.js`.** Same discipline on the FAQ: no timelines asserted — the answer to "كم يستغرق" points at the free study rather than naming weeks. | Noted — client input needed |
| 2026-08-19 | **Section ground alternates deliberately across the finished page:** hero `#f5f6f8` → platforms dark `#0b0b0d` → process white → pricing `#f5f6f8` → features white → faq `#f5f6f8`. Features was flipped from `#f5f6f8` to white when Pricing landed, to stop two same-colour bands sitting adjacent. **Keep this rhythm in mind before changing any section's background.** | Noted |
| 2026-08-19 | **App Development page — motion pass ("make it alive").** Applied the Motion Lab across the whole page with **no new dependency** — scroll binding is framer-motion's `useScroll`, so GSAP was not installed. New shared pieces: `components/Common/Magnetic.js` (magnetic hover), `components/Common/ParticleField.js` (canvas particle network — IntersectionObserver-paused, DPR-capped, single static frame under reduced motion). Per section: Hero gets the particle field + ambient glow, scroll parallax on both columns, three glass badges at real `translateZ` depths inside the tilt, a screen-wake scan sweep and a scroll cue; new `TechMarquee` ticker band; Platforms gets `DeviceCube` (CSS-3D cube, six DOM faces, rotation bound to scroll) plus per-card `Tilt3D` and a sheen sweep; Process gets a scroll-linked rail that fills brief→launch, plus live artwork on hover; new `Stats` count-up strip; Pricing gets the **border beam** on the popular plan (the lab's #1 recommendation, and this was its named home) with a staggered checklist; Features/FAQ get hover states. `pages/services/app-development/index.js` now also renders `TechMarquee` and `Stats`. | Done — pending visual review with user |
| 2026-08-19 | **Reduced-motion support added (was the one gap flagged in the lab review).** `Reveal` and `Tilt3D` now branch on `useReducedMotion` and render final-state content instead of animating; every new component does the same; a `@media (prefers-reduced-motion: reduce)` guard at the end of `styles/style.scss` kills the CSS keyframes (marquee, beam, glow, shimmer, float). Also fixed a real bug in `Tilt3D`: `useTransform` was being called inside a `glare && (...)` branch — a conditional hook — now hoisted via `useMotionTemplate`. | Fixed |
| 2026-08-19 | **⚠️ Node.js is not installed on this machine** (`node`/`npx` absent; `C:\Program Files\nodejs` does not exist, though it is still on PATH). So `npx sass` could not run and the dev server could not be started — the motion pass is **unverified in a browser**. The compiled CSS for it was written by hand into `styles/style.css` to match the SCSS block appended to `styles/style.scss`; **recompile with `npx sass styles/style.scss styles/style.css --no-source-map` once Node is back**, and re-check the page visually before shipping. | Blocked — needs Node |
| 2026-08-20 | **Cube replaced — user: "the cube is really bad, change it totally".** Deleted `components/AppDev/DeviceCube.js` and the abstract labelled cube entirely. Replaced with `components/AppDev/AppOrbit.js`: six phone screens standing on a ring in 3D (`rotateY(i·60deg) translateZ(250px)`), each showing a small abstract app UI and an Arabic caption for a real app type (متجر / محفظة / حجز / توصيل / لوحة تحكم / تواصل) — so the 3D moment now shows *what we build* instead of a decorative box. Same mechanic as before: still CSS 3D + DOM text (0 KB, correct Arabic shaping), ring rotation still bound to scroll via `useScroll`, `backface-visibility: hidden` hides the far half so it never appears mirrored, three accent tones stop it reading as one repeated screen. Narrow screens scale the perspective stage (0.82 / 0.56) rather than recomputing the radius, which is set in px from the component. | Done — pending visual review with user |
| 2026-08-20 | **Orbit is now grab-and-spin.** `AppOrbit` takes two rotation inputs that simply sum: scroll position (as before) plus a pointer drag. Pointer events with `setPointerCapture` so the gesture survives leaving the element; horizontal drag → `dragRotate` motion value at 0.35°/px; on release a flick above 20°/s continues with framer-motion's `type: "inertia"` and decays to a stop, a slow release just stops. Dragging never cancels the scroll contribution — the two add, so letting go mid-page doesn't snap the ring back to a "correct" angle. Stage gets `cursor: grab`/`grabbing`, `user-select: none` (a drag must not become a text highlight), and `touch-action: pan-y` so vertical page scrolling still works on touch while horizontal drags rotate. Reduced motion: drag still works, inertia and the scroll binding don't. Removed the now-unused `.is-static` ring rule. Hint text updated to "اسحب لتدوير النماذج — أو مرّر الصفحة". | Done — pending visual review with user |
| 2026-08-19 | **SEO page — Stage 2 (Structure) done.** New route `pages/services/seo/index.js` + `components/Seo/` (11 gray-box sections + `wireframeStyles.js`) + `data/seo/structure.md`. Section list was **chosen from research, not invented**: reviewed WebFX `/seo/services/` (the most complete reference — 18 sections), Victorious `victorious.com/services/`, and Backlinko `/seo-services` for the canonical pillar breakdown. Order: Hero → Free audit → Results → Service pillars (6) → AI search/GEO → Process (4) → Case studies → Reporting → Pricing → FAQ → Final CTA. Verified 200 with all 11 sections present. | Done — pending structure review with user |
| 2026-08-19 | **Wired two dead links.** `components/Layouts/Navbar.js` ("سيو") and `components/Layouts/Footer.js` ("تحسين محركات البحث سيو") were **both already pointing at `#`** — placeholders waiting for exactly this page. Both now point at `/services/seo/`, and the navbar item got its `currentPath` active-state check filled in to match. | Done |
| 2026-08-19 | **Research note — GEO/AEO is now a required section.** Every 2026 source consulted treats visibility inside Google AI Overviews / ChatGPT as no longer optional on an SEO service page; WebFX gives it a dedicated section. Built in as section 5 — it is also the clearest differentiator against older local competitors. | Noted |
| 2026-08-19 | **Reuse note — the site already has Arabic SEO copy.** `components/Common/SeoShowcase.js` (live on the homepage, `pages/index.js`) already states five pillars: تحليل الكلمات المفتاحية · تحسين السيو الفني · بناء الروابط الخلفية · تحسين محتوى الصفحات · تقارير أداء شهرية. **Reuse these verbatim in the UI phase rather than rewriting them** — the services grid and the reporting section were both structured around them. | Noted |
| 2026-08-19 | **SEO wireframe flags blocked content in amber, not grey.** Added a `needsData` style to `components/Seo/wireframeStyles.js` so the three blocks that cannot be written without the client (Results figures, Case studies, Pricing model) are visually distinct from ordinary placeholders and cannot quietly ship as if finished. Open questions are listed at the bottom of `data/seo/structure.md`. | Noted — client input needed |
| 2026-08-20 | **Digital Marketing page — Stage 2 (Structure) done.** New route `pages/services/digital-marketing/index.js` + `components/DigitalMarketing/` (11 gray-box sections + `wireframeStyles.js`) + `data/digital-marketing/structure.md`. Section list chosen from research: WebFX `/digital-marketing/services/` (18 sections; flow = credibility → education → services → social proof → differentiation → comparison → objection handling → conversion) and Digital Agency Network's 2026 services list (26 tracked service types; most in-demand: web design 22%, web dev 20%, SEO 20%, strategy 19%, social 19%). Order: Hero → Trust strip → Channels (6) → Strategy (4) → Social media → Why KaTechs → Results → Reporting/ROI → Pricing → FAQ → Final CTA. Verified 200 with all 11 sections present. | Done — pending structure review with user |
| 2026-08-20 | **Route decision (asked user, they chose new route).** A live page already existed at the misspelled `/services/degital-market/`, linked from `components/Services/Services.js`, while the navbar + footer "تسويق إلكتروني" items were dead (`#`). User picked a clean `/services/digital-marketing/`. Done: old page deleted (`pages/services/degital-market/`, backup at `/tmp/degital-market-index.js.bak`); Navbar, Footer and `Services.js` all repointed; **a permanent redirect `/services/degital-market` → `/services/digital-marketing` added to `next.config.js`** so the old URL does not 404. The old page's *components* were deliberately left in place — they are the copy source for the UI phase and `WhyChooseUs` is shared with other pages. | Done |
| 2026-08-20 | ⚠ **The redirect is NOT yet verified live.** `next.config.js` changes need a dev-server restart, and node is unavailable in this environment (see the 2026-08-19 note), so the old URL still returned 404 when tested. **Re-test `/services/degital-market/` after the next dev-server restart** — it should 308 to `/services/digital-marketing/`. Config uses the no-trailing-slash form of both paths, which is correct under `trailingSlash: true`, but that is reasoning, not a live check. | Needs verification |
| 2026-08-20 | **IA decision — no GEO/AI section on the marketing page.** `/services/seo/` already owns the AI Overviews / GEO section; repeating it here would put two of our own pages in competition for the same intent — the exact duplicate-content problem the routing decision was made to avoid. Instead the SEO card in the channels grid **cross-links to `/services/seo/`**. | Noted |
| 2026-08-20 | **Reuse note — real Arabic marketing copy already exists.** Reuse verbatim in the UI phase rather than rewriting: `components/Services/digital-service.js` has the four strategy steps (دراسة السوق والمنافسين · تحديد الجمهور المستهدف بدقة · اختيار القنوات الأنسب · تنفيذ حملات مدفوعة وعضوية); `components/digitalfeature/WhatWeOffer.js` has the social-media offering (خطة محتوى شهرية · تصميم منشورات · التفاعل مع الجمهور) plus the platform list; `components/degital-markiting-details/ServiceDetailsContent.js` has the intro paragraph and the existing `/digital-market-order` CTA target. Sections 4 and 5 were structured around these. | Noted |
| 2026-08-20 | **Training page (التدريب) — Stage 2 (Structure) done.** New route `pages/training/index.js` + `components/Training/` (12 gray-box sections + `wireframeStyles.js`) + `data/training/structure.md`. Order: Hero → Stats → **Course catalog** → Learning formats → Learning path → Instructors → Outcomes → Certificate → Upcoming schedule → Pricing → FAQ → Enrol CTA. Verified 200 with all 12 sections. | Done — pending structure review with user |
| 2026-08-20 | **Audience decision (asked user).** Zero training content existed anywhere in the repo, so there was no signal to infer from. User chose **B2C — public courses for individuals** over corporate/team training. This makes it a **catalog + enrolment** page, not a quote-based service page, and it is **the only page on the site aimed at a different buyer** (students/juniors, not business owners) — tone can be lighter than the service pages during the UI phase. | Done |
| 2026-08-20 | **Route decision — `/training/`, not `/services/training/`.** Training is not one of the IT services sold to businesses, and the navbar treats "التدريب" as a **top-level item**, a sibling of الخدمات rather than a child. Matches the other top-level pages (`/jobs`, `/offers`, `/support`). | Noted |
| 2026-08-20 | Research basis for the catalog: **General Assembly `/students/courses`** — its card carries topic tag(s), title, 1–2 sentence description, course-type icon, delivery method, duration, link arrow; filter bar is Topic/Location/Schedule/Format. Section 3 is modelled on this, plus level/start date/price which a paid B2C catalog needs and GA defers to the course detail page. **Coursera `/degrees`** for discovery patterns. The 2026 bootcamp roundups (Course Report, Forbes, Boot.dev) all show **delivery format** (live cohort vs self-paced + mentorship) is the axis buyers compare on — hence section 4 is its own block, not a card field. | Noted |
| 2026-08-20 | ⚠ **Training page is the most content-blocked of the three.** Unlike SEO and digital marketing, **there is no existing in-repo copy to reuse** — nothing on this page can go past wireframe into real copy until the client supplies the actual course list. Six open questions in `data/training/structure.md`; five sections are amber-flagged (stats, catalog, instructors, outcomes, schedule, pricing). | Noted — client input needed |
| 2026-08-20 | Wired two more dead links: navbar "التدريب" and footer "تدريب" → `/training/`. Also fixed a **pre-existing** dead footer link spotted in passing: "تطبيقات الجوال" was still `#` despite `/services/app-development/` existing — now pointed at it. | Done |
| 2026-08-20 | **Stage 2.5 — "functional UX" pass on SEO + Digital Marketing + Training.** User explicitly chose **functional UX only, no visual styling** over full themed design. All three pages rebuilt from gray boxes into real layouts with real hierarchy, real copy, working interactions and responsive rules — **deliberately greyscale, no brand colour, no shadows, no Cairo, no motion**, so the flow can be judged before the visual pass. All three verified 200, zero `wireframe-` leftovers. `wireframeStyles.js` deleted from all three component folders. | Done — pending flow review with user |
| 2026-08-20 | **New file `styles/ux-prototype.css`** — the shared neutral layer for the three prototypes; every class prefixed `ux-`. Imported once in `pages/_app.js` under a comment marking it temporary. **To start the visual phase: delete the file, remove that one import, restyle — nothing else depends on it.** Covers section shell, type scale, buttons, cards/grids, split layouts, lists, stat tiles, tags, form controls, filter bar, table, a neutral `react-accessible-accordion` skin, and the amber `.ux-flag` for client-blocked content. | Done |
| 2026-08-20 | **Interactions built for real, because the interaction is what needs reviewing:** SEO free-audit form (validation, error states, success state — submits nowhere yet, per open question 4); Training course catalog (three working filters + live result count + empty state + load-more paging); real accordions on all three FAQs. Training's placeholder courses are explicitly commented as **structural placeholders for exercising the filter, not a proposed curriculum.** | Done |
| 2026-08-20 | **Copy written at UX level is grounded, not invented.** SEO pillars reuse `SeoShowcase.js`; marketing strategy steps reuse `digital-service.js`; social-media block reuses `WhatWeOffer.js`. Every figure that would be a factual claim is left as `00` / `— —` behind an amber flag. Two FAQ answers on the Training page are deliberately **written as open questions rather than answers** (refund policy, whether live sessions are recorded) so they cannot ship as accidental promises. The certificate section says "شهادة إتمام" only and **claims no external accreditation**, pending question 5. | Noted |

| 2026-08-20 | **SEO page — visual design pass, all 11 sections.** Reference: `public/images/seo page inspiration/*.png` (5 images: Hero, stats row, dark 6-card grid, split badge+heading, dark 3-card grid). Built a **new, page-specific design system** — flat black/white editorial (oversized Cairo headline, thin-border pill badges/buttons, alternating white/light-gray/near-black `#0c0c0e` section bands) — deliberately *not* the soft-emboss recipe used on Hosting/Emails/AppDev, same "distinct treatment per page on the shared shell" pattern as the rest of the site. New `.seo-*` classes in `styles/style.scss` (~700 lines); replaces `.ux-*` classes on this page only — `styles/ux-prototype.css` stays imported for Digital Marketing/Training, untouched. All content/copy preserved from the Stage-2.5 UX prototype (including client-blocked amber flags on Results/Case Studies/Pricing) — this was a skin pass, not a content rewrite. Section→reference mapping: Hero=1st section, Results=numbers section, Pillars=3rd section (dark 6-card), AiSearch=4th sec (split), CaseStudies=cards.png (dark 3-card); AuditForm/Process/Reporting/Pricing/Faq/CtaBand have no reference image — built in the same established system. Motion: `Reveal`/`staggerParent`/`staggerItem` throughout, `Magnetic` on primary CTAs, small CSS loop animations (SERP-badge float, AI-answer shimmer, report-bar grow-in) all covered by a new `prefers-reduced-motion` block. Verified with a real headless-browser pass (Playwright, installed temporarily then removed — net zero dependency change) after two real bugs found and fixed (see below). | Done — pending visual review with user |
| 2026-08-20 | **Bug found + fixed: `.seo-first` padding never defined.** Hero content rendered at `top:0`, directly under the fixed navbar, and — as a side effect — sat inside the dead zone of `Reveal`'s `-80px` viewport margin, so it *never* fired its scroll-reveal and stayed invisible (`opacity:0`) indefinitely. Fixed by adding the same 160px/130px/110px top-clearance convention already used by `.ux-first`/`.app-hero`/hosting's hero. **Lesson: any new page's first section needs this — it's easy to forget since nothing errors, the section just silently never becomes visible.** | Fixed |
| 2026-08-20 | **Confirmed: `Reveal`'s `whileInView` genuinely works correctly** once elements are geometrically normal — verified by scrolling a real headless browser through the whole page (not just measuring a `fullPage` screenshot, which resizes the viewport once and does *not* reliably fire every `IntersectionObserver` the way real incremental scrolling does — several sections read as blank in a naive `fullPage` capture despite being fine for an actual visitor). **If a future visual check via screenshot shows blank sections, scroll incrementally first before assuming a real bug** — the PricingFlow opacity bug (2026-08-19) was real; this was not. | Noted |
| 2026-08-20 | Hit the same `.next` **corrupted-cache-for-one-route** class of bug already logged twice before (2026-08-19, App Dev and Emails) — deleting only the route's compiled artifacts and touching the source file returned a 404 rather than recompiling; a real content edit (not just `touch`) was needed to force Next's watcher to notice. **Updated gotcha: prefer a real (even trivial) file edit over `touch` to force recompilation in this environment.** | Fixed |
| 2026-08-20 | **Hosting Services — motion pass ("make it more live"), all 4 sections.** Brought the page up to the same Motion Lab standard already shipped on Emails/AppDev, reusing the exact same shared components rather than inventing new ones: `Reveal`/`staggerParent`/`staggerItem` (replaces the old `data-aos` throughout — Hero text, pricing title, pricing cards, cPanel banner text, feature grid items all now scroll-reveal with stagger), `Tilt3D` on the hero image (with the domain badge pinned at a real `translateZ(40px)` depth via `preserve3d`) and the cPanel banner image, `Magnetic` on the hero/cPanel CTA buttons and each pricing card's CTA, and a CSS-only rotating border-beam ring (`.hosting-pricing-beam`, same mechanic as `.app-plan-beam`/`.email-plan-beam`, reusing the shared `appBeamSpin` keyframe) around the popular pricing plan. Feature-grid icons get a small hover scale on top of the existing color/accent-bar hover. | Done — pending visual review with user |
| 2026-08-20 | **Real CSS conflict found + fixed: wrapping `.hosting-pricing-card` in a motion.div broke its existing hover-lift.** The card's CSS had `&:hover { transform: translateY(-6px) }` and the popular variant had a static `transform: scale(1.05)` — both fight framer-motion, which also drives `transform` via inline style once the card is a `motion.div` (inline style always wins over a stylesheet rule, hover or not). Fixed by removing both CSS `transform` hover rules (hover-lift now lives entirely in JS via `Reveal`'s `whileHover={{y:-8}}`, matching the AppDev/Emails convention) and switching the popular card's static enlargement from `transform: scale(1.05)` to the standalone CSS `scale: 1.05` property, which composes independently of `transform` instead of colliding with it. **Lesson for any future card wrapped in a motion.div: any existing `transform`-based CSS on that element (hover or static) needs to move to `scale`/`rotate`/`translate` (the standalone properties) or into the motion component's own props — plain `transform` in CSS will lose the fight.** | Fixed |
| 2026-08-20 | **Real bug found + fixed: `.hosting-hero` was missing `position: relative`.** Unlike `.app-hero`/`.email-hero` (which have it), `.hosting-hero` only had `overflow: hidden`. The new `.hosting-hero-glow` (`position: absolute`, ambient pulse) still painted correctly clipped (nothing was visibly wrong, and `window.scrollTo` proved there was no real scrollable overflow) but — lacking a positioned ancestor to resolve its containing block against — it inflated `document.documentElement.scrollWidth` by ~150px, fluctuating with its own pulse animation. Root-caused by comparing against `.app-hero` (which doesn't have this issue) rather than trusting the scrollWidth number at face value. Fixed by adding `position: relative` to `.hosting-hero`, matching the other hero sections. **Gotcha for any future `position: absolute` decorative element: the section it's visually anchored to needs its own `position: relative`, not just `overflow: hidden` — otherwise its containing block silently resolves further up the tree.** | Fixed |
| 2026-08-20 | Also hit the `.next` corrupted-cache-for-one-route bug a third time on this page, plus the shared dev server (owned by a concurrent session) went down entirely mid-check (no process on :3000) — waited, then started a clean single instance once confirmed nothing else was listening, per the existing "one clean instance" guidance. Confirmed the earlier hosting-services responsive fix (`.section-title` min-width→max-width) and the App Dev overflow/hydration fixes still hold after the restart — no regressions. | Noted |

## New Page Workflow (agreed process — follow for every new page)

Each new page is built in 3 stages, walked through together before moving to the next:

1. **Understand** — what service/topic this page is for, the page's goal, what content/information needs to be displayed.
2. **Structure** — page structure, UX draft (rough flow of sections), list of elements/components needed.
3. **Design** — section-by-section UI design, then the full page UI/UX put together.

Do not skip ahead to design before structure is agreed, and don't skip structure before the goal/content is agreed.

## Data Folder Convention

Each service page gets **one flat folder directly under `data/`**, named after its route (matches the folder under `pages/services/`) — not nested under a literal `services/` folder. That one folder holds everything for all 3 stages of that page, separate from any component markup:

- `brief.md` — Stage 1 deliverable: **starts with a short "Summary — the actual service data" block** (plan names, prices, features, selling points — the real content, not a list of section names), then page goal, audience, full section-by-section content, open questions. Reviewed with the user before Stage 2.
- `data.js` — same content as a structured JS module (plain objects/arrays), scaffolded for later import by new components in Stage 3. Not wired into pages until the new page is actually built.
- (Stage 2/3 will add more files to the same folder, e.g. `structure.md`, `design.md` — TBD as we reach those stages.)

```
data/
  hosting-services/   ← populated (brief.md, data.js)
  emails/              ← populated (data.js only — page came from a sketch, not a Stage 1 brief)
  app-development/     ← populated (data.js only — page came from inspiration images)
  seo/                 ← populated (structure.md — Stage 2 wireframe done)
  digital-marketing/   ← populated (structure.md — Stage 2 wireframe done)
  training/            ← populated (structure.md — Stage 2 wireframe done)
  vps-hosting/         ← placeholder README, not started
  wordprees-hosting/   ← placeholder README, not started
```

## Conventions to Follow for New Work

- New pages: `pages/<route>.js` (thin) + `components/<PageName>/*.js` (page-specific sections), same as existing pages — do not introduce a new folder structure or styling system (no CSS Modules, no styled-components, no Tailwind) unless explicitly requested.
- Reuse `components/Layouts/Navbar.js` + `Footer.js` for the page shell.
- Reuse `components/Common/*` for generic sections (CTA, Testimonials, PageBanner, etc.) where it fits; only build new one-off components for the parts that need the new UX treatment.
- Keep RTL in mind (Arabic site) — verify new markup/styles work with `styles/rtl.css`.
- Don't add new hardcoded secrets — use env vars for anything sensitive.
